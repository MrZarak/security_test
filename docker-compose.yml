version: "3.9"

services:
  postgres:
    container_name: postgres
    image: postgres
    ports:
        - "5431:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
volumes:
  security_test_volume: